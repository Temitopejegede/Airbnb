import React from 'react';
import { View, Text } from "react-native"

const GuestsScreen = (props) => {
    return (
        <View>
            <Text>notJust a components</Text>
        </View>
    );
};

export default GuestsScreen;